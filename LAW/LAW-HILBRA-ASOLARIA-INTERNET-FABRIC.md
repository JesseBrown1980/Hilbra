# LAW-HILBRA-ASOLARIA-INTERNET-FABRIC

```
LAW-DESCRIPTOR
law_id         : LAW-HILBRA-ASOLARIA-INTERNET-FABRIC
short_name     : THE NAMING OF ASOLARIA AS THE INTERNET FABRIC = HILBRA
pid            : 604f473ac0981dd7
hilbert        : 1643
bh             : BH.tauH.L16.D49.100988019
authority      : OP-JESSE-DANIEL-BROWN (apex) + FOUNDATION-V3 quorum cosign window
birth          : 2026-06-22
substrate      : Asolaria federation multi-machine fabric (two colonies live)
classification : CLASS-1 FOUNDATIONAL
supersedes     : nothing (additive)
status         : ACTIVE (operator-apex-ratified) -- UNVERIFIED-as-canon until quorum cosign
```

## 1. Canonical definition

**Asolaria IS the internet fabric.** Its canonical name is **Hilbra** ("The address that finds every fabric").

- Umbrella name **Hilbra** — a coined contraction of **Brown‑Hilbert**, the space‑filling curve on which every PID is placed. That one continuous universal index IS the "Google for fabric‑matrixes": walk the curve to find any artifact, agent, or joined fabric, anywhere.
- Public membership layer **Glyphnet** — the BEHCS‑1024 glyph substrate a newcomer learns to become legible to the network.

## 2. Design laws (binding on every Hilbra node)

1. **Rust‑canonical** — the canonical node is Rust on Host‑8 (Linux/Ubuntu/WSL, toward a 2TB OS‑on‑metal). Node/JS may prototype; it is not canonical. The policy core (HMAC link‑auth + the level tagger) is already Rust.
2. **Pixels before GPU** — processing is bytes/pixels‑first on CPU/metal, **zero GPU dependency**, so any device can become a surface.
3. **16‑level sharing + PII‑by‑consent** — a member may share at any of the 16 levels, including PII, **only by explicit owner consent + a freshly issued scoped capability key**. The public tier (level 0) is provably PII‑free.
4. **Key‑off‑wire** — keys/secrets never cross the wire and never live in any public repo; minted locally or delivered out‑of‑band under governance.
5. **Web‑of‑trust, not a central CA** — root of trust is the append‑only cosign chain + peer vouches; no node holds a master key that mints membership.
6. **No fire without operator T0** — every canon promotion + substrate change needs quorum cosign + a per‑action human T0; `auto_fire_allowed` stays false.

## 3. Honesty clause

Claimed‑as‑arrived for **nothing** beyond the live two‑colony link. Everything from a third colony onward is aspirational, built in the gated phases of `ROADMAP.md`. Every claim carries a provenance grade (MEASURED / CANON / UNVERIFIED); no bare "MEASURED" without evidence.

## 4. Chain anchor

Operator‑apex‑ratified at birth; becomes fully canon only when the quorum (5‑of‑5) cosign verdict is appended to the cosign chain. Until then: ACTIVE, UNVERIFIED‑as‑canon.
